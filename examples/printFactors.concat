#+
Prints all prime factors of a given number
+#
'stringBuilder' *import
'print' *import
## define overloaded <+ operator to simplify string building
overload: <+ sb.appendC sb.appendS i64.sbAppend end

proc( stringBuilder i64 -> stringBuilder ) =>: appendFactors
  $ 0 < if -_ end =:: mut: n ## ensure number is non-negative
  2:i64 while $ $ * n <= do =:: p
    while n p u% 0 == do
      ' ' <+ p <+
      n p / n=
    end
  finally p 1 + end ..drop
  n 1 > if ' ' <+ n <+ end
end
proc( i64 : n -> ) =>: printFactors
  256 sb.create
    "the factors of " <+ n <+ " are:" <+
    n appendFactors '\n' <+
    $ sb.asStr io.print
  sb.free ..drop
end

proc( -> ) =>: main
  2 printFactors
  42 printFactors
  64 printFactors
  1001 printFactors
  2381 printFactors
end
