import optional

proc( i32 optional : n -> bool ) =>: isEven
  n ? and n .data 1 & ? ! end return
end

proc( -> ) =>: main
  i32 optional.empty isEven ! assert "empty is not even"
  5 optional.wrap isEven ! assert "5 is not even"
  -16 optional.wrap isEven assert "-16 is even"
end
