import optional

proc( i32 optional : n -> bool ) =>: isEven
  n ? and n optional.unwrap 1 & ? ! end return
end

proc( -> ) =>: main
  i32 optional.empty isEven ! "empty is not even" assert
  5 optional.wrap isEven ! "5 is not even" assert
  -16 optional.wrap isEven "-16 is even" assert
end
