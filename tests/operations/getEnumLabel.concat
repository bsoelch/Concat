enum( A B C ) =:: anEnum
union( i32 : A _ : B f64 : C ) =:: anUnion

proc( -> ) =>: main
  anEnum .A =:: a1
  anEnum .A =:: a2
  anEnum .C =:: c1
  a1 ..label a2 ..label == assert "a1 and a2 have same label"
  a1 ..label anEnum .B ..label != assert "a1 and anEnum .B do not have the same label"
  a1 ..label c1 ..label != assert "a1 and c1 do not have the same label"

  1 anUnion .A =:: u1
  2 anUnion .A =:: u2
  3.0 anUnion .C =:: u3
  u1 ..label u2 ..label == assert "u1 and u2 have same label"
  u1 ..label anUnion .B ..label != assert "u1 and anEnum .B do not have the same label"
  u1 ..label u3 ..label != assert "u1 and u3 do not have the same label"
end
