import debug
proc( char8 : x -> char8 ) =>: f
  5 while ..dup 0 >= do
    x ..dup switch
      'x' case ..drop 1 - continue
      default ..swap ..drop return break
    end
  end ..drop
  'X' return
end

proc( -> ) =>: main
  'a' f debug.print
  'x' f debug.print
  'z' f debug.print
end
