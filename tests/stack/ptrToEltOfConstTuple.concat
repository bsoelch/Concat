struct( i32 : mut: a i32 : b ) =:: struct

proc( -> ) =>: main
  0 0 struct new =:: s
  s@ .a
  s@ .b
  ..?types
  ..drop.2
  s .a 0 == "" assert
  s .b 0 == "" assert
  0 0 struct new =:: mut: mutS
  mutS@ .a
  mutS@ .b
  ..?types
  ..drop 1 ..swap =[]
  mutS .a 1 == "" assert
  mutS .b 0 == "" assert

  s@ =:: sP
  sP .a
  sP .b
  ..?types
  ..drop.2
  sP [] .a 0 == "" assert
  sP [] .b 0 == "" assert

  mutS@ =:: mutSP
  mutSP .a
  mutSP .b
  ..?types
  ..drop 2 ..swap =[]
  mutSP [] .a 2 == "" assert
  mutSP [] .b 0 == "" assert
end
