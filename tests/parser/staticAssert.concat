1 1 + 2 == "1+1=2" assert
"Hello" ..length 5:iSize == "string length" assert
1 0 > "ordering" assert

enum( A B C D E ) =:: anEnum

anEnum ..length 5:iSize == "enum length" assert

i32 2 3 array ..length 3:iSize == "multi-dim. array length" assert

proc( -> ) =>: main end
