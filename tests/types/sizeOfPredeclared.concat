type : later
struct( later : x later : y ) =:: point

proc( -> ) =>: main
  1 later cast 2 later cast point new =:: p
  p @ =:: pointPointer
  pointPointer [] .y 2 later cast == assert "pointPointer [] .y  2 =="
  point ..type.size 8 == assert "sizeof(point) = 8"
end
i32 =:: later
