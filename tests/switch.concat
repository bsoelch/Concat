enum( A B i32 : C i32 ptr : D ) =:: anEnum

proc( i32 : i anEnum : e char : c => ) =: switchTest
  i switch
    1 2 case
      return
    3 case
      break
    0 case
      false if return else break end
      ## break is not needed if all branches terminate
    -1 case
      false if break else break end
      break ## break is allowed if all branches terminate
    default
  end
  e switch 
    A B case
      break
    C case
      e .C print
      break
    D case
      e .D print
      break
  end
  c switch ## switch character
  ' ' '\n' '\t' '\v' '\f' '\r' case
    true
    break
  default
    false
  end =:: isSpace
  isSpace print
end

entryPoint:
 0 anEnum .A 'A' switchTest
 1  0 anEnum .C new  ' ' switchTest
 0 =:: localInt
 0xabcdef  localInt addrOf anEnum .D new  '\n' switchTest
end
