#include stringIO

## TODO allow passing arguments to tests
entryPoint:
  0 while #dup ..argc < do
    #dup ..getArg string.create io.stdOut #swap io.fputStr #drop
    io.stdOut '\n' io.fputc #drop
    1 +
  end #drop
end

## TODO automatically convert label at end of if to enum-value
