
DECLARE PROC 2 I64 I64 TUPLE 2 I64 I64 proc0 
  RETURN CONST I64 0 CONST I64 1
END
DECLARE PROC 0 VOID doNothing
  
END
DECLARE PROC 1 UNION 2 I32 I64 PROC 1 I64 I64 typeTest
  
END

START 
  DECLARE I64 A
    ADD 
      CONST I64 1 
      CONST I64 1
  DECLARE I64 B
    MULTIPLY
      SUBTRACT
        GET A
        CONST I64 1
      CONST I64 3
      
  PRINT I64 GET A
  PRINT I64 GET B
  SET A NEG GET B
  
  DECLARE TUPLE 2 I64 I64 tmp CALL proc0 CONST I64 1 CONST I64 2
  CALL doNothing
  SET_ELEMENT 0 GET tmp GET_ELEMENT 1 GET tmp 

  IF 
    GET A
    
    PRINT I64 CONST I64 1  
  ELIF 
   AND2
     GET B 
     XOR
       GET A
       CONST I64 42
       
    PRINT I64 CONST I64 2
  ELSE

    PRINT I64 CONST I64 3
  END

  DECLARE I64 C 
    CONST I64 10
    
  WHILE 
    GET C

  PRINT I64 GET C
  SET C
    SUBTRACT 
      GET C
      CONST I64 1
      
  END
  
  PRINT BOOL CONST BOOL 1
END

DECLARE STRING s0 "Strings are \"ignored\"  "
# Comments are now enabled
DECLARE CHAR c0 'A' 
DECLARE STRING s1 "This is a unicode\u2122 multi-line
 string"
